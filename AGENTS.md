# 圆宝项目1：AI 工作规则

这份文件供以后在本文件夹中新建的 Codex 对话读取。目标是让每次制作测试都沿用同一套简单流程，用户不需要管理技术细节。

## 先读什么

1. 先读根目录 `项目说明.md`。
2. 修改某套测试前，再读 `测试项目/<slug>/项目说明.md` 和 `test.manifest.json`。
3. 涉及服务器、兑换码或上线时，再读 `docs/03-deployment-runbook.md`。
4. 涉及 Windows Docker 故障时，读 `docs/04-windows-docker-recovery.md`。

## 固定边界

- `测试项目/` 中每个文件夹是一套独立测试，可以自由设计题目、选项、算法、页面、图片和交互。
- `server/`、`apps/admin/`、`packages/test-sdk/` 是多测试共享底座，负责兑换码、管理后台、授权和服务运行。
- 不要为了统一而强迫不同测试使用相同的题数、结果算法或页面模板。
- 内容合规与版权检查不属于本项目流程，不在项目内增加相关步骤或文档。
- 兑换码、生产数据库、密码和密钥不得提交到 GitHub，也不得写进普通文档或源代码。

## 用户工作流

默认只在本地工作：

1. 用户说明想制作的测试。
2. AI 在 `测试项目/` 下新建或修改对应的小项目，并同步维护该测试的 `项目说明.md`。
3. AI 在本地完成构建、自动检查和浏览器验收，给用户本地预览地址。
4. 用户亲自体验并提出修改，继续在本地迭代。
5. 只有用户明确表示“本地验收通过，可以上传”或“可以正式开放”，才允许提交/推送 GitHub、构建生产镜像或改动 Sealos。

“帮我看看”“继续修改”“本地运行”“生成测试”等说法都不构成上线授权。

## 新建测试

- 使用 `npm run test:new -- --slug <英文标识> --name "<中文名称>" --mode standard` 建立骨架，再按创意需要深度修改。
- 特殊玩法可以使用 `custom-static`，也可以建立自定义 React 页面；必须保留 `test.manifest.json` 并接入共享授权 SDK。
- 每套测试至少包含：`test.manifest.json`、可运行代码、`项目说明.md`。
- 本地原始素材、生成记录和兑换码可放进该测试的 `本地资料/`；这些内容默认不会进入 GitHub 或 Docker 镜像。
- 新测试加入 `tests-registry.json` 后，运行测试包校验、类型检查、平台构建和自动测试。

## 发布规则

- 正式发布使用 Docker 镜像。当前默认由 GitHub Actions 在 Linux 上构建并推送到 GHCR，再由 Sealos 拉取。
- 发布前可先检查本机 Docker；Docker 正常时允许本地做镜像验证，但不能因 Docker Desktop 故障长时间阻塞发布。
- 若日志命中已知 Windows AF_UNIX socket 问题，只进行一次标准快速恢复；仍失败则保留现场并改用 GitHub Actions 构建。
- 发布前备份生产数据库，发布后检查 `/health/ready`、管理后台和所有测试入口。
- Git 操作保持简单：本地草稿不要求用户管理分支或版本；用户验收后由 AI 完成一次清晰的备份提交和发布记录。

## 修改纪律

- 不删除或覆盖用户原始素材、兑换码、数据库及历史迁移包。
- 不执行恢复出厂设置、删除 Docker 数据、重建生产数据库等高风险操作，除非用户明确授权且已有备份。
- 文档随实现同步更新，尤其是根目录 `项目说明.md`、对应测试的 `项目说明.md` 和实施清单。
- 保持说明面向非技术用户；必要技术细节放在 `docs/`，不要堆到根目录。
