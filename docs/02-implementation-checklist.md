# 实施清单

> 本清单必须随实现推进更新。`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成并验证。

## A. 基线与设计

- [x] 盘点月下心笺代码、授权服务和 Sealos 构建流程。
- [x] 盘点内在三声部的题目、算法、结果和静态页面。
- [x] 确认系统边界、三种测试接入模式和第一阶段验收标准。
- [x] 将现有扁平仓库整理为可维护的应用、服务和文档结构。

## B. 数据与服务端

- [x] 引入 SQLite、版本迁移和事务仓储层。
- [x] 建立 tests、test_versions、code_batches、licenses、admin_sessions、audit_logs。
- [x] 实现兑换码加密、摘要、生成、导入、分配、兑换、校验、停用和解绑。
- [x] 实现旧 licenses.json 幂等迁移和明文清单补全。
- [x] 实现管理员登录、安全 Cookie、会话失效和登录限速。
- [x] 实现 `/health/live`、`/health/ready` 和优雅退出。
- [x] 实现自动备份、保留策略、手动备份和完整性检查。

## C. 公网管理后台

- [x] 登录与退出。
- [x] 总览统计。
- [x] 测试项目与版本列表。
- [x] 批量生成和导入兑换码。
- [x] 订单号原子分配与买家回复复制。
- [x] 兑换码/订单查询、禁用和设备解绑。
- [x] 批次导出、审计日志和备份状态。
- [x] 桌面与手机端布局验证。

## D. 测试包与生成器

- [x] 定义并校验 `test.manifest.json`。
- [x] 实现共享浏览器授权 SDK。
- [x] 实现新测试脚手架命令。
- [x] 实现注册表、重复 slug、资源和敏感信息校验。
- [x] 提供标准配置模式运行时和评分策略。

## E. 两套测试迁移

- [x] 月下心笺迁入 `moonlit-fate` 测试包，保留根路径兼容。
- [x] 月下心笺改用按测试隔离的新授权 API。
- [x] 内在三声部迁入 `inner-voices` 测试包。
- [x] 内在三声部增加授权门禁，不改变原题目、算法和视觉体验。
- [x] 验证两套测试的完成流程、历史结果和跨测试授权隔离。

## F. 构建、部署与上线

- [x] 更新 Dockerfile、CI 镜像发布和 `.dockerignore`。
- [x] 编写 Sealos 环境变量、Secret、卷和探针配置说明。
- [x] 本地执行单元、API、并发、迁移和前端端到端测试。
- [x] 构建并推送生产镜像；GitHub Actions 生产流水线通过。
- [x] 保留旧 `licenses.json`，并在明文补全前自动备份迁移后的 SQLite 数据库。
- [x] 更新 Sealos 镜像、环境变量和卷配置，`/health/ready` 返回 `200`。
- [x] 自动迁移旧授权状态，并补全原 1000 个明文兑换码：完成 1000、重复 0、无效 0。
- [x] 为内在三声部生成、导出并核对 1000 个独立兑换码。
- [x] 公网验收后台、月下心笺和内在三声部的授权门禁与试题加载。
- [x] 重启 Sealos 应用后再次核对数据库统计、批次、备份和就绪状态。

## G. 2026-09-10 上线结果

- 生产镜像：`ghcr.io/penguinkingking/moonlit-fate-quiz:9fa15f66a04d39beeea13a2b5b5cdd10ee19aa0c`。
- Sealos：固定 1 实例，CPU `200m`，内存 `256Mi`，`/app/data` 持久卷 `1Gi`。
- 数据库：2 套测试、2006 条授权记录；月下心笺可发放 999、历史已兑换 6、仅摘要旧码 1；内在三声部可发放 1000。
- 备份：手动迁移后备份和导入前自动备份均可在后台查看；当前数据库完整性检查通过。
- 公网验收：根路径标题已为“月下心笺 · 寻找你的命定角色”；后台可登录；内在三声部可解锁并进入第 1/48 题。
- 持久性：重启后 Pod 为 `running`、重启次数 0，健康检查保持 `200`，数据库和批次统计未变化。
