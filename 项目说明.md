# 圆宝项目1

这是“批量制作测试小游戏”的主文件夹。它把每套测试的创意和页面，与兑换码、后台、服务器部署等共用能力分开管理。

你平时只需要记住两件事：

1. 想做新测试时，在这个文件夹里新建一个 Codex 对话，直接说明测试想法。
2. 本地体验满意后，明确告诉 AI：`本地验收通过，可以上传并正式开放。`

在你发出第 2 句话以前，AI 只进行本地设计、修改和测试，不上传 GitHub，也不改线上网站。

## 现在有什么

| 项目 | 本地文件夹 | 线上入口 | 状态 |
| --- | --- | --- | --- |
| 月下心笺 | `测试项目/moonlit-fate/` | `/`、`/tests/moonlit-fate/` | 已上线 |
| 内在三声部 | `测试项目/inner-voices/` | `/tests/inner-voices/` | 已上线 |
| 超级 SBTI | `测试项目/super-sbti/` | `/tests/super-sbti/` | 已上线 |
| 此刻充电方式 | `测试项目/quick-recharge/` | `/tests/quick-recharge/` | 已上线 |
| 统一管理后台 | `apps/admin/` | `/admin/` | 已上线 |

## 文件夹怎么分

```text
圆宝项目1/
  项目说明.md              你首先看的总说明
  AGENTS.md                新对话自动遵守的工作规则
  测试项目/                每套测试都是这里的一个小项目
    moonlit-fate/           月下心笺
    inner-voices/           内在三声部
    super-sbti/             超级 SBTI
    quick-recharge/         此刻充电方式
  apps/admin/              所有测试共用的管理后台
  server/                  兑换码、登录、数据库和网站服务
  packages/                测试共用的授权组件
  scripts/                 新建测试、构建和校验工具
  docs/                    详细设计、实施和上线说明
  本地归档/                原迁移包、临时验证和历史材料，不上传
```

每套测试文件夹里都有自己的 `项目说明.md`、代码和页面资源。原始图片、生成记录及兑换码放在该测试的 `本地资料/`，它们只保留在本机，不进入 GitHub 或 Docker 镜像。

## 创建一个新测试

不需要自己建代码文件，也不需要填写固定表格。直接在新对话中说明，例如：

> 我想做一个 20 题的旅行性格测试，结果有 6 类。整体明亮、有杂志感，先帮我设计题目、结果和页面，在本地运行给我看，不要上传。

AI 会建立新的测试文件夹和项目说明，与你确定内容与设计，接入共用兑换码，在本机运行并根据你的反馈继续修改。

## 本地验收与正式发布

本地阶段使用普通网页构建和预览，速度最快。正式发布阶段才使用 Docker。你确认“可以正式开放”后，AI 触发一条自动流水线，不需要你登录 Sealos 或执行命令：

```text
设计与修改 -> 本地预览 -> 你验收通过
                         |
                         v
完整检查 -> GitHub 备份 -> 固定版本镜像 -> 数据库备份
       -> Sealos 更新 -> 手机浏览器完整答题 -> 成功
                                      |
                                      v
                                  失败自动回滚
```

正常发布通常只需要几分钟。每个可能卡住的步骤都有等待上限：镜像最多等 5 分钟，Sealos 更新最多等 3 分钟，整次发布最多 15 分钟。失败时继续使用上一个正常镜像，并把具体阻塞步骤留给 AI 处理。

发布验收使用一枚永久保留的系统专用码。它不会发给买家，每次验收结束都会恢复为未绑定状态，不消耗普通库存。

Docker Desktop 在 Windows 11 上出现已知 socket 启动故障时，AI 按 `docs/04-windows-docker-recovery.md` 快速处理；本机 Docker 仍不可用时，直接让 GitHub 的 Linux 环境完成镜像构建。

## 重要数据

- 生产兑换记录和后台数据位于 Sealos 持久化卷，不在代码仓库中。
- 本机兑换码清单位于各测试的 `本地资料/兑换码/`。
- 管理员密码和系统密钥只保存在部署 Secret 中，不写入项目说明。
- GitHub 只保存最小权限的发布凭据：它只能读取和更新这一个 Sealos 应用，不能读取 Sealos Secret，也不能创建或删除资源。
- `本地归档/` 保存整理前的原始迁移资料，不参与日常开发。

更详细的技术资料位于 `docs/`。日常使用只读本文件即可。
